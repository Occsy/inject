[package]
name = "injekt"
version = "1.3.0"
edition = "2024"
description = "A library that writes to a .dat file as key, value in binary format"
license = "MIT"

[features]
experimental = []

[dependencies]
